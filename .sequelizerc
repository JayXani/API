const {resolve} = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), //file of configuration
  'models-path': resolve(__dirname, 'src', 'models'), //What is the path model
  'migrations-path': resolve(__dirname,'src','database','migrations'),  //Where's location of migrations
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds')
};
